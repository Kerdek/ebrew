# "chub.hb"

export format(fmt@ i8 ...)@ i8 = return
   cast @i8 0 is buf
   cast %i64 0 is buflen
   &buflen is b
   open_memstream &buf b is out
   (cast @va_list __va_area__)@ is ap
   (vfprintf out fmt &ap)
;: fclose out
;: buf
;;
